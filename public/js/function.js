$(document).ready(function(){
    breakMessage();
})

function breakMessage(){
    // var message = $("break-message").val();
    // var maxCharacterInLine = 20;
    // // var messages = message.split(" ");
    // console.log(message);
    // // if(message. > maxCharacterInLine){

    // }
}